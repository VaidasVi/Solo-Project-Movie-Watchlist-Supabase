# Netlify build configuration

[build]
  command = "npm run build"
  publish = "dist"

# Disable secrets scanning
# These keys are designed to be public and used client-side (Supabase anon key, OMDB API key)
# They are protected by Row Level Security policies and rate limiting
[build.environment]
  SECRETS_SCAN_ENABLED = "false"
